import { DiscoverSections } from "@/features/feed/DiscoverSections";

/** Discover（§10.7 MVP 弱实现）：三个分区复用 getFeed 各 tab 数据。 */
export default function DiscoverPage() {
  return <DiscoverSections />;
}
